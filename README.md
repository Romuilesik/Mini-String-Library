# Mini-String-Library
Header only C++23 string utility library. Trimming, splitting, case conversion, padding, wildcard matching and more - all the small stuff the standard library leaves out. No dependencies, single file
